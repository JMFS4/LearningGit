#include<stdio.h>
/// @brief programa que exibe no ecran  a tabuada do 5, apenas para os números pares
/// @return 
int main()
{
    int i ;
for(i=1;???; i++) 
    {
        if( ??? == 0 ) // divisão inteira
        {
            printf(" 5 * %d = %d\n", i, i* 5 );
        }
        else
        {
            //????;
        }
    }
    return 0;
}
